# ── Python ──────────────────────────────────────────
venv/
__pycache__/
*.pyc
*.pyo

# ── Node / 프론트엔드 빌드 ────────────────────────────
client/node_modules/
client/dist/

# ── 데이터베이스 (방송 데이터 — 업로드 제외) ──────────
db/
*.db

# ── 생성 리포트 (앱이 자동 생성) ──────────────────────
reports/

# ── 로컬 샘플/데이터 파일 ─────────────────────────────
data/

# ── OS / 에디터 ───────────────────────────────────────
.DS_Store
Thumbs.db
*.log

# ── 업로드 스크립트 (PAT 토큰 포함 — 절대 업로드 금지) ─
upload_to_github.sh

# ── FTP/EC2 접속 정보 포함 스크립트 (업로드 제외) ──────
ftp_to_ec2.sh
ftp_to_ec2_dayinput.sh
wsl_to_ec2_rsync.sh

# ── 보안 키 (SSH/EC2 개인키 — 절대 업로드 금지) ────────
*.pem
*.key
id_rsa
id_rsa.pub

# ── 문서 (업로드 제외) ────────────────────────────────
README.md
