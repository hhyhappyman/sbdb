# ── Python ──────────────────────────────────────────
venv/
__pycache__/
*.pyc
*.pyo

# ── Node / 프론트엔드 빌드 ────────────────────────────
client/node_modules/
client/dist/

# ── 데이터베이스 (방송 데이터 — 업로드 제외) ──────────
db/
*.db

# ── 생성 리포트 (앱이 자동 생성) ──────────────────────
reports/

# ── 로컬 샘플/데이터 파일 ─────────────────────────────
data/

# ── OS / 에디터 ───────────────────────────────────────
.DS_Store
Thumbs.db
*.log

# ── 셸 스크립트: install_service.sh 만 업로드, 나머지 전부 제외 ─
#   (upload/ftp/rsync/setup 등 접속정보·토큰 포함 가능성 → GitHub 제외)
*.sh
!install_service.sh

# ── 보안 키 (SSH/EC2 개인키 — 절대 업로드 금지) ────────
*.pem
*.key
id_rsa
id_rsa.pub

# ── 문서 (업로드 제외) ────────────────────────────────
README.md

# ── 매뉴얼 생성 산출물 (캡처 스크립트에 비밀번호 포함) ─
capture_shots.py
make_manual.py
manual_shots/
*.docx
